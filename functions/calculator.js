export default class Calculator {
    constructor() {
    }

    static subtract(x, y) {
        return x - y;
    }

    static divide(x, y) {
        return x / y;
    }

    static multiply(x, y) {
        return x * y;
    }
}